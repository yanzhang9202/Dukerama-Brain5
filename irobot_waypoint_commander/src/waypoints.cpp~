// Track multiple waypoints, camera orienting to the initial guess of target position, taking raw images at each waypoint.
// Created by Yan Zhang, on Sep 11st, 2015.

#include "wpslib.cpp"

int main(int argc, char **argv)
{
  ros::init(argc, argv, "irobot_wps_commander");
  ros::NodeHandle node; 

  loadWaypoints();
  loadTargets();

  std::cout << InitTargets << std::endl;

//  // MAIN LOOP
//  while( ros::ok() )
//  {
//  }

  return 0;
}

